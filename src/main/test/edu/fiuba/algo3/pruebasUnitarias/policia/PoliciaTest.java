package edu.fiuba.algo3.pruebasUnitarias.policia;

import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

import clases.Ciudad;
import clases.Coordenadas;
import clases.Policia;
import clases.rangos.Detective;
import clases.rangos.Novato;

public class PoliciaTest {

	@Test
	public void policiaNovatoTiempoInicialEsCero() {
		/*

		// Creo ciudad
		Coordenadas coordenadasMexico = new Coordenadas(23.634501, -102.552784);
		Ciudad mexico = new Ciudad("Mexico", coordenadasMexico);

		// Creo un policia de rango Detective...
		Policia policia = new Policia(new Novato(), mexico);
		assertEquals(0, policia.tiempoTranscurridoEnHoras());
	}

	@Test
	public void policiaDetectiveTiempoInicialEsCero() {

		// Creo ciudad
		Coordenadas coordenadasMexico = new Coordenadas(23.634501, -102.552784);
		Ciudad mexico = new Ciudad("Mexico", coordenadasMexico);

		// Creo un policia de rango Detective...
		Policia policia = new Policia(new Detective(), mexico);
		assertEquals(0, policia.tiempoTranscurridoEnHoras());

		 */
	}
}
